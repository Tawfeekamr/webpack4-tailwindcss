module.exports = {
  theme: {
    fontFamily: {
      // light: ['Exanple font Light', 'sans-serif'],
      // body: ['Exanple font Regular', 'sans-serif'],
      // bold: ['Exanple font Bold', 'sans-serif'],
    },
    colors: {
      // Example for add new colors
      // pink: '#FF0058',
      // yellow: '#FAD848',
      // gray: '#535B6C',
      // light: '#AAB3C2',
      // slight: '#E7EBF2',
      // white: '#fff',
      // red: '#F50000',
      // softblack: '#0A0E20'
    },
    container: {
      center: true,
    },
    extend: {
      // Extend will extend tailwind default data, you can use it also with fontFamily, fontSize, colors ...etc
      screens: {
        // Example for add new breakpoints
        // '2xl': '1400px',
        // '3xl': '1600px',
        // '4xl': '1900px',
      },
      fontSize: {
        // Example for add new fontsizes
        // '8xl': '6rem',
      },
    },
  },
  variants: {},
  plugins: [],
};
